version: '3.8'

services:
  umrah-operations:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: umrah-operations-app
    restart: unless-stopped
    ports:
      - '80:80'
    environment:
      - NODE_ENV=production
